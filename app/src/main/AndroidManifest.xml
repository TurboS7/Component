<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fourc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FourC">
        <activity android:name=".MSQLiteActivity"></activity>
        <activity android:name=".MSharedPreferencesActivity" />
        <activity android:name=".MSaveMainActivity" />
        <activity android:name=".MFourCActivity" />
        <activity android:name=".MContentProviderActivity" />
        <activity android:name=".MBroadcastReceiverActivity" />
        <activity android:name=".MServiceActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>